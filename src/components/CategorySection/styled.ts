import styled from 'styled-components'
import _Carousel  from 'react-elastic-carousel'

export const Title = styled.h2`
   
    font-style: normal;
    font-weight: 500;
    font-size: 24px;
    line-height: 36px;
    margin-bottom: 19px;
    text-transform: capitalize;

`

export const Carousel = styled(_Carousel)`
    user-select: none !important;
    margin-bottom: 20px;
   
`
