import styled from "styled-components";

export const Contatainer = styled.section`
    display: flex;
    flex-wrap: wrap;
    gap: 25px;
    justify-content: center;


`

export const WrapperButton = styled.div`
    width: 100%;
    display: flex;
    justify-content: center;

`

export const Button = styled.button`
    width: 300px;
    height: 50px;
    background-color: var(--strong_cyan);
    border-radius: 25px;
    border: none;
    color: var(--white);
    font-size: 20px;
    font-weight: bold;
    cursor: pointer;
    text-align: center;

`
